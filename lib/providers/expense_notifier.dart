import 'package:flutter/material.dart';

class ExpenseNotifier extends StatelessWidget {
  const ExpenseNotifier({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}