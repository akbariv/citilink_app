import 'package:flutter/material.dart';

class ExpenseState extends StatelessWidget {
  const ExpenseState({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}